# Backend dependencies.
#
# Pinned to the exact versions verified in the `backing` conda environment
# (Python 3.11+, verified on 3.12) so fresh installs reproduce the working set.
# For a fully reproducible environment (transitive closure included) install
# requirements.lock instead:
#   pip install -r requirements.lock
#
# mootdx is installed from the vendored copy in ./vendor/mootdx because the
# PyPI release pins httpx<0.26, which conflicts with the app's httpx 0.28.x;
# the vendored package relaxes that bound (metadata-only patch, see
# vendor/mootdx/README.md).

fastapi==0.141.1
uvicorn[standard]==0.52.3
sqlalchemy==2.0.52
pymysql==1.2.0
cryptography==50.0.0
baostock==0.9.3
akshare==1.18.91
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.2
pandas==3.0.5
numpy==2.5.2
requests==2.34.2
scipy==1.18.0
scikit-learn==1.9.0
torch==2.13.0
alembic==1.19.1
pytest==9.1.1
httpx==0.28.1
slowapi==0.1.10
itsdangerous==2.2.0
langchain-core==1.5.4
langchain-openai==1.5.1
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
./vendor/mootdx
